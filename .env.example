# example config for demiurg
# copy to ./.demiurg or ~/.demiurg/config

# number of parallel workers (default: 4)
NUM_WORKERS=4

# target directory for operations (default: .)
TARGET_DIR=.

# data directory for state (default: {TARGET_DIR}/.demiurg)
# DATA_DIR=./.demiurg

# log directory (default: {TARGET_DIR}/.demiurg/log)
# LOG_DIR=./.demiurg/log

# port for future web UI (default: 8080, currently unused)
PORT=8080
