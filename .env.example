# project-local environment variables for demiurg
# copy to .env in project root and customize

# number of parallel workers (default: 4)
NUM_WORKERS=4

# target directory for operations (default: current directory)
TARGET_DIR=.

# uncomment to override defaults:
# DATA_DIR=./.demiurg
# LOG_DIR=./.demiurg/log
# PORT=8080
