# project-local environment variables for ship
# copy to .env in project root and customize
#
# CLI flags override env vars:
#   -w, --workers     NUM_WORKERS
#   -t, --timeout     TASK_TIMEOUT
#   -m, --max-turns   MAX_TURNS

# number of parallel workers (default: 4)
# NUM_WORKERS=4

# task execution timeout in seconds (default: 1200)
# TASK_TIMEOUT=1200

# max agentic turns per task (default: 25)
# limits token usage by capping API round-trips
# MAX_TURNS=25
